
## 一、总体结构概述

系统分为**惯性系控制层（Inertial Frame）**和**机体系控制层（Body Frame）**两部分，控制信号由外向内依次传递，控制频率逐级提高，从低频位置控制到高频角速率控制。最终输出推力与舵面（或电机）控制指令 ( $T_{sp}$ )。

---

## 二、惯性系控制层（Inertial Frame）

该部分主要实现飞行器在空间坐标系中的位置与速度控制，典型更新频率为 **50 Hz**。

1. **位置控制（Position Control, P 控制）**

   * 输入：位置设定值 ( $X_{sp}$ )（例如 ( $[x_{sp}, y_{sp}, z_{sp}]$ )）。
   * 输出：速度设定值 ( $V_{sp}$ )。
   * 控制律：采用比例控制（P 控制器），根据当前位置误差生成期望速度指令。

2. **速度控制（Velocity Control, PID 控制）**

   * 输入：速度设定值 ( $V_{sp}$ )。
   * 输出：加速度设定值 ( $A_{sp}$ )。
   * 控制律：采用 PID 控制器，使飞行器速度跟踪目标速度，输出期望加速度以修正偏差。

3. **加速度与偏航到姿态转换（Acceleration and Yaw to Attitude）**

   * 输入：加速度设定值 ( $A_{sp}$ ) 和偏航角设定值 ( $\psi_{sp}$ )。
   * 输出：姿态设定值（四元数形式）( $q_{sp}$ )。
   * 功能：将期望的空间加速度与偏航角转化为飞行器姿态设定（滚转角、俯仰角、偏航角），用于姿态控制环。

---

## 三、机体系控制层（Body Frame）

该层在机体坐标系下工作，主要负责姿态与角速度控制。控制频率较高，**姿态控制 250 Hz**，**角速率控制 1 kHz**。

1. **角度控制（Angle Control, P 控制）**

   * 输入：姿态设定值 ( $q_{sp}$ )。
   * 输出：角速度设定值 ( $\Omega_{sp}$ )。
   * 控制律：采用比例控制，使当前姿态跟踪目标姿态，生成期望角速度指令。

2. **角速度控制（Angular Rate Control, PID 控制）**

   * 输入：角速度设定值 ( $\Omega_{sp}$ )。
   * 输出：控制量增量（例如电机推力、舵面偏角等）：

     * ( $\delta_{A_{sp}}$ )：副翼控制量（Aileron）
     * ( $\delta_{E_{sp}}$ )：升降舵控制量（Elevator）
     * ( $\delta_{R_{sp}}$ )：方向舵控制量（Rudder）
     * ( $\delta_{T_{sp}}$ )：推力控制量（Throttle）
   * 控制律：采用 PID 控制器，确保飞行器角速度精确跟踪，补偿姿态扰动。

---

## 四、Mixer（混控器）

所有控制通道输出的控制量（($\delta_{A_{sp}}$, $\delta_{E_{sp}}$, $\delta_{R_{sp}}$, $\delta_{T_{sp}}$)）输入混控器，由其根据机型配置（如四旋翼、固定翼或VTOL）计算最终的执行机构命令：
$[
T_{sp} = \text{Mixer}(\delta_{A_{sp}}, \delta_{E_{sp}}, \delta_{R_{sp}}, \delta_{T_{sp}})
]$
Mixer 将控制量分配到具体的电机转速、舵面偏角或推力通道。

---

## 五、控制层级与频率总结

| 控制层级        | 控制内容      | 控制器类型 | 频率     |
| ----------- | --------- | ----- | ------ |
| 位置控制        | 位置 → 速度   | P     | 50 Hz  |
| 速度控制        | 速度 → 加速度  | PID   | 50 Hz  |
| 加速度与偏航 → 姿态 | 坐标变换      | —     | 50 Hz  |
| 姿态控制        | 姿态 → 角速度  | P     | 250 Hz |
| 角速率控制       | 角速度 → 控制量 | PID   | 1 kHz  |

---

## 六、系统工作流程概括

1. 根据目标位置 ( $X_{sp}$ ) 生成期望速度 ( $V_{sp}$ )。
2. 由速度控制器计算期望加速度 ( $A_{sp}$ )。
3. 将加速度与偏航角 ( $\psi_{sp}$ ) 转换为姿态目标 ( $q_{sp}$ )。
4. 姿态控制器生成角速度目标 ( $\Omega_{sp}$ )。
5. 角速率控制器计算舵面或推力控制量。
6. Mixer 分配并输出最终的执行机构命令 ( $T_{sp}$ )。
